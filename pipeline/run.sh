
#!/bin/bash
# Runs the pipeline.
# Assumes pdftk and pdfocr containers can be crated and started. Assumes also a
# volume container exist to hold intermediate data.
# run.sh - runs the pipline. Assumes docker containers can be started. Assumes container volume exists to share data.
